# GEMINI_API_KEY: Required for Gemini AI API calls.
# AI Studio automatically injects this at runtime from user secrets.
# Users configure this via the Secrets panel in the AI Studio UI.
GEMINI_API_KEY="MY_GEMINI_API_KEY"

# APP_URL: The URL where this applet is hosted.
# AI Studio automatically injects this at runtime with the Cloud Run service URL.
# Used for self-referential links, OAuth callbacks, and API endpoints.
APP_URL="MY_APP_URL"

# The password required to authenticate as an admin in the client (stored in Firestore or as a secret)
VITE_ADMIN_PASSWORD="admin123"

# Firebase Project ID for database persistence
VITE_FIREBASE_PROJECT_ID="the-vagina-room"

# Firebase Configuration as JSON string (Used by the client SDK)
# Expected Value: The complete JSON config object copied from your Firebase Project Settings -> Web App Setup
VITE_FIREBASE_CONFIG='{"apiKey": "...", "authDomain": "...", "projectId": "...", "storageBucket": "...", "messagingSenderId": "...", "appId": "...", "firestoreDatabaseId": "..."}'

# Public Cloudinary config for client-side direct uploads
VITE_CLOUDINARY_CLOUD_NAME=""
VITE_CLOUDINARY_UPLOAD_PRESET="ml_default"

# Server-side Cloudinary credentials for secure operations/deletions/signing
CLOUDINARY_CLOUD_NAME=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# Google Analytics tracking ID for frontend user analytics
VITE_GA_TRACKING_ID=""

